# تصميم واجهة منصة تعليمية

هذا المشروع هو عبارة عن تصميم واجهة أمامية (Frontend) لصفحة هبوط لمنصة تعليمية متكاملة، مستوحى من تصميم ووظائف منصة "فيو كلاس" (ViewClass). تم بناء الواجهة باستخدام HTML, CSS, و JavaScript وهي مصممة لتكون حديثة، جذابة، ومتجاوبة مع جميع الشاشات.

## ✨ رابط العرض المباشر (Live Demo)

يمكنك مشاهدة النسخة المستضافة من الموقع مباشرة على الرابط التالي:

**[سيتم وضع رابط GitHub Pages هنا بعد النشر]**

*(ملاحظة: سيتم تفعيل هذا الرابط بعد رفع المشروع على GitHub Pages)*

---

## 🚀 المميزات الرئيسية

- **تصميم عصري ونظيف:** واجهة مستخدم بسيطة تركز على المحتوى وسهولة الاستخدام.
- **متجاوب بالكامل (Fully Responsive):** يعمل بشكل ممتاز على أجهزة الكمبيوتر المكتبية، الأجهزة اللوحية، والهواتف المحمولة.
- **شريط تنقل ثابت (Sticky Navigation):** يبقى شريط التنقل ظاهرًا في أعلى الصفحة عند التمرير.
- **قائمة منسدلة للجوال:** قائمة "برجر" تفاعلية تظهر في الشاشات الصغيرة لتسهيل التنقل.
- **تأثيرات حركية بسيطة (Transitions):** تأثيرات CSS بسيطة عند التفاعل مع الأزرار والبطاقات لإعطاء تجربة مستخدم أفضل.
- **كود منظم:** تم فصل الهيكل (HTML)، التصميم (CSS)، والتفاعلية (JavaScript) لسهولة القراءة والتعديل.

---

## 🛠️ التقنيات المستخدمة

- **HTML5:** لهيكلة محتوى الصفحة.
- **CSS3:** لتصميم وتنسيق الواجهة، مع استخدام متغيرات CSS (CSS Variables) لسهولة تغيير الألوان.
- **JavaScript (ES6):** لإضافة التفاعلية إلى شريط التنقل في نسخة الجوال.
- **Font Awesome:** لاستخدام الأيقونات في قسم المميزات والفوتر.
- **Google Fonts:** لاستخدام خط "Cairo" المناسب للغة العربية.

---

## 📖 كيفية الاستضافة على GitHub Pages

استضافة هذا الموقع مجانية وبسيطة جدًا باستخدام GitHub Pages. اتبع الخطوات التالية:

1.  **إنشاء مستودع جديد (New Repository):**
    - اذهب إلى حسابك في GitHub واضغط على `New repository`.
    - أعطِ المستودع اسمًا (مثلاً `educational-platform-ui`).
    - اجعله `Public` (عام).
    - اضغط `Create repository`.

2.  **رفع ملفات المشروع:**
    - في صفحة المستودع الجديد، اضغط على `uploading an existing file`.
    - اسحب وأفلت ملف `index.html` الذي أنشأته (أو جميع الملفات إذا فصلتها).
    - اكتب رسالة `commit` (مثلاً "Initial commit") ثم اضغط `Commit changes`.

3.  **تفعيل GitHub Pages:**
    - اذهب إلى `Settings` (إعدادات) المستودع.
    - في القائمة الجانبية، اختر `Pages`.
    - تحت قسم `Branch`، اختر `main` (أو `master`) من القائمة المنسدلة ثم اضغط `Save`.

4.  **انتظر قليلاً!**
    - بعد دقيقة أو دقيقتين، سيظهر رابط موقعك في نفس الصفحة. سيكون على هذا الشكل:
      `https://<your-username>.github.io/<repository-name>/`